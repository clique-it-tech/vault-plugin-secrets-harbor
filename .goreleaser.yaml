version: 2

builds:
  - id: plugin
    main: ./cmd/vault-plugin-secrets-harbor
    binary: vault-plugin-secrets-harbor-{{ .Os }}-{{ .Arch }}
    env:
      - CGO_ENABLED=0
    goos: [linux, darwin]
    goarch: [amd64, arm64]
    flags: [-trimpath]
    ldflags:
      - -s -w -X main.version={{ .Version }}

archives:
  - formats: [binary]
    name_template: "{{ .Binary }}"

checksum:
  name_template: SHA256SUMS

changelog:
  use: github
